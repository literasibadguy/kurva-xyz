{
  "name": "kurva.xyz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all gulp eleventy --parallel watch:gulp eleventy:serve",
    "eleventy": "eleventy --quiet",
    "eleventy:serve": "eleventy --serve",
    "gulp:sass": "npx gulp sass",
    "gulp": "npx gulp build",
    "watch:gulp": "npx gulp build --silent &&  gulp watch --silent"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@11ty/eleventy-img": "^2.0.0"
  },
  "dependencies": {
    "gorko": "^0.9.1",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "js-yaml": "^4.1.0",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.70.2",
    "sass": "^1.51.0"
  }
}
